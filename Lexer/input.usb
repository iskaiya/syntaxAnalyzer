wala ugat() {
    bilang x = 5    // Missing semicolon
    bilang y        // Missing assignment and semicolon
    z = 10;         // Undeclared variable (lexer might catch this)
    kung (x > 5 {   // Missing )
        ani(x);
    // Missing }
    bilang a = 1;
}