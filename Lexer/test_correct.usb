wala ugat() {
bilang a;
bilang a, b;
lutang x = 3.9;
lutang y = x;
z = x + y * 2;

kwerdas msg = "Hello USBong";
bulyan flag = tama;
    
ani(z);
ani("Hello Usbong!");
ani("Hello", x);

tanim(a, b);

    kung (y == 20) {
        z = 100;
    } kundiman (y > 15) {
        z = 50;
    } kundi {
        z = 0;
    }

    para (bilang counter = 0; counter < 10; counter = counter + 1) {
        i = i + counter;
        ani(i);
    } 

    para (bilang i = 0; i < 10; i = i + 1) {
      para (bilang j = 0; j < 10; j = j + 1) {
         ani(i + j);
      }
    ani("/n");
    }


habang (x > 0) {
        y = x + 10;
        ani(x);
    } 
    
    habang (x < 100) {
        y = x + 10;
        ani(x);
    }

    gawin {
        y = y - 1;
        ani(y);
    } habang (y > 0);
    
    bilang a, b, c = 5;
    tanim(a, b);
    c = a + b;
    ani(c);
    
    kung (a >= b) {
        ani(a);
    }
    
    kung (a <= b) {
        ani(b);
    }
    
    kung (a != b) {
        ani("Not equal");
    }
}

