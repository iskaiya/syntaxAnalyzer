wala ugat() {

bilang a;
bilang a, b;

x = 3.9;
y = x;
z = x + y * 2;
a = b = c = 5;
c = a + b;
msg = "Hello USBong";
    
ani(z);
ani("Hello Usbong!");
ani("Hello", x);
ani(c);

tanim(a, b);

    kung (y == 20) {
        z = 100;
    } kundiman (y > 15) {
        z = 50;
    } kundi {
        z = 0;
    }

    bilang counter;
    para (counter = 0; counter < 10; counter = counter + 1) {
        i = i + counter;
        ani(i);
    } 

    bilang i, j;
    para ( i = 0; i < 10; i = i + 1) {
      para ( j = 0; j < 10; j = j + 1) {
         ani(i + j);
      }
    }


habang (x > 0) {
        y = x + 10;
        ani(x);
    } 

    gawin {
        y = y - 1;
        ani(y);
    } habang (y > 0);
        
    kung (a >= b) {
        ani(a);
    }
    
    kung (a <= b) {
        ani(b);
    }
    
    kung (a != b) {
        ani("Not equal");
    }
}

